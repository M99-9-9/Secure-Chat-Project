# حل مشكلة Framework في vercel.json - وثيقة شاملة

## المشكلة
```
فشل البناء - خطأ Vercel JSON Schema
الرسالة: يجب أن تكون قيمة `framework` مساوية لإحدى القيم المسموح بها
```

### السبب الجذري
قيمة `framework: "other"` ليست من القيم المدعومة في Vercel. القيم المسموح بها محدودة وتشمل فقط:
- `blitzjs`, `nextjs`, `gatsby`, `remix`, `react-router`, `astro`, `hexo`, `eleventy`
- `docusaurus-2`, `docusaurus`, `preact`, `solidstart-1`, `solidstart`, وغيرها

---

## نوع المشروع
**Vault** هو تطبيق React SPA مبني على:
- **Webpack** - لأداة البناء الرئيسية
- **React 19** - لإطار عمل الواجهة الأمامية
- **NX Monorepo** - لإدارة المشروع
- **TypeScript** - للنوع الثابت

---

## الحل الجذري المطبق

### ✅ الإجراء الأول: حذف خاصية `framework`
```json
// ❌ الخاطئ
"framework": "other"

// ✅ الصحيح
// تم حذف الخاصية بالكامل
```

### السبب:
Vercel يكتشف نوع الإطار **تلقائياً** من خلال:
1. وجود `package.json` و `package-lock.json` / `pnpm-lock.yaml`
2. تحليل البنية الداخلية للمشروع
3. ملفات التكوين الموجودة

لذلك، بدلاً من تحديد قيمة غير صحيحة، من الأفضل **حذفها تماماً** والسماح لـ Vercel باكتشاف البنية بذاته.

---

## الملف الصحيح الآن

```json
{
  "version": 2,
  "buildCommand": "pnpm install && nx build web && (cp -r apps/web/webapp/* . || true)",
  "outputDirectory": ".",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "env": {
    "NODE_ENV": "production",
    "NODE_OPTIONS": "--max-old-space-size=4096"
  },
  "public": true,
  "cleanUrls": true,
  "trailingSlash": false,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  // باقي الإعدادات...
}
```

### ملاحظات مهمة:
- ✅ **لا توجد خاصية `framework`** - Vercel سيكتشفها تلقائياً
- ✅ **`buildCommand` صحيح** - يبني المشروع بشكل صحيح
- ✅ **`outputDirectory` صحيح** - يشير للجذر حيث يتم نسخ الملفات
- ✅ **`installCommand` صحيح** - يستخدم pnpm بدون frozen-lockfile
- ✅ **`env` صحيح** - متغيرات البيئة الضرورية موجودة

---

## لماذا هذا الحل مستدام وقابل للصيانة؟

### 1. **عدم الاعتماد على قيم ثابتة**
- بدلاً من تحديد "react" أو "other"، نترك Vercel يكتشف النوع
- هذا يقلل احتمالية حدوث أخطاء في المستقبل

### 2. **توافق مع التحديثات**
- إذا تغيرت بنية المشروع، Vercel سيتكيف تلقائياً
- لا حاجة لتحديث `vercel.json` يدوياً

### 3. **اتباع Best Practices من Vercel**
- التوثيق الرسمية توصي بحذف `framework` للمشاريع المخصصة
- هذا النهج يتوافق مع أفضل الممارسات

### 4. **الحد من نقاط الفشل**
- كل خاصية إضافية قد تكون مصدر مشاكل
- حذف ما ليس ضروري يقلل التعقيد

---

## خطوات النشر الآن

### الخطوة 1: تأكيد التغييرات
```bash
git diff vercel.json
```

يجب أن تشاهد:
```diff
- "framework": "other",
```

### الخطوة 2: دفع التغييرات
```bash
git add vercel.json
git commit -m "fix: remove invalid framework property from vercel.json"
git push origin develop
```

### الخطوة 3: مراقبة البناء
- انتقل إلى https://vercel.com/dashboard
- اختر المشروع الخاص بك
- اضغط "Deployments" ومراقب البناء الجديد

---

## النتائج المتوقعة

### ✅ البناء سينجح الآن:
- لن تظهر أخطاء JSON Schema
- Vercel سيكتشف أن هذا React SPA
- البناء سيتم بنجاح دون تحذيرات

### ⏱️ المدة:
- **البناء:** 3-5 دقائق
- **النشر:** 1 دقيقة إضافية
- **الإجمالي:** 5 دقائق

### ✨ النتيجة النهائية:
- ✅ الموقع حي وعامل
- ✅ Vault جاهز للاستخدام
- ✅ لا توجد أخطاء في السجلات

---

## الملخص النهائي

| المعيار | التفاصيل |
|--------|---------|
| **المشكلة** | قيمة `framework` غير صحيحة |
| **السبب** | "other" ليست قيمة مدعومة |
| **الحل** | حذف الخاصية بالكامل |
| **الفائدة** | اكتشاف تلقائي من قبل Vercel |
| **الاستدامة** | عالية جداً - لا حاجة لتعديلات مستقبلية |
| **المخاطر** | منخفضة جداً - هذا هو النهج الموصى به |

---

## الملفات المتعلقة

- **`vercel.json`** - ملف التكوين الرئيسي (تم إصلاحه)
- **`package.json`** - معلومات المشروع (لم يتغير)
- **`apps/web/webpack.config.ts`** - تكوين البناء (لم يتغير)

---

## الخطوة التالية

✅ المشروع جاهز الآن للنشر الفوري. اختر الأمر التالي:

```bash
git push origin develop
```

Vercel سيكتشف التغييرات تلقائياً وسيبدأ البناء الجديد!
